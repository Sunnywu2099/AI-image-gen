由于安全限制，我无法直接访问当前项目文件夹（`/Users/sunny.wu/Desktop/ai-image-gen-main`）**以外**的目录（即无法访问 `...-main-2`）。

请您执行以下任一操作，以便我获取代码：

1.  **复制文件**：请将您同事项目中的 API 代码文件（例如 `app/api/image/route.ts`）**复制**到当前项目文件夹的根目录下，并重命名为 **`route_v2.ts`**。
2.  **直接粘贴**：或者，直接打开那个文件，全选代码并**粘贴**发送给我。

一旦文件在当前目录下，我就可以读取它，提取新的 API 调用逻辑（包含 `private_key` 等鉴权部分），并将其合并到我们现有的代码中。